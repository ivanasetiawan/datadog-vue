# Datadog Vue Plugin - 01. Setup #
Step by step:
- Create a basic `package.json` and .gitignore
- Add devDependencies that includes [Babel](https://babeljs.io/), and [Eslint](https://www.npmjs.com/package/eslint)
- run `npm install`